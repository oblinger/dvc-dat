# DVC-Dat Test Configuration
# This configures where test Dats are stored during test runs.

# Where test dats live (relative to this file's directory)
dat_folders: test_sync_folder/

# Imported when the config installs; the test mounts live there
main: mounts
